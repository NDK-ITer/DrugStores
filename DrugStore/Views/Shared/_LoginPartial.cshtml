@*@using Microsoft.AspNetCore.Identity
@using DrugStore.Areas.Identity.Data

@inject SignInManager<AppNetUser> SignInManager
@inject UserManager<AppNetUser> UserManager*@




<ul class="navbar-nav">
@if (SignInManager.IsSignedIn(User))
{
    <li class="nav-item">
        <a id="manage" class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">@UserManager.GetUserName(User)</a>
    </li>
    <li>
        @if (User.IsInRole("Admin".ToUpper().Trim()))
        {
            @Html.ActionLink("Quản lý", "Index", "HomeAdmin", new { area = "Admin", title = "Manage", @class = "nav-link" }, null)
        }
    </li>
    
    <li class="nav-item">
        <form id="logoutForm" class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
            <button id="logout" type="submit" class="nav-link btn btn-link text-dark border-0">Đăng xuất</button>
                
        </form>
       
    </li>
        
}
else
{
   @* <li class="nav-item">
        <a class="nav-link text-dark" id="register" asp-area="Identity" asp-page="/Account/Register">Register</a>
    </li>*@
    @*<li class="nav-item">*@
        <a id="login" asp-area="Identity" asp-page="/Account/Login"><i class="fa fa-user"> Đăng Nhập</i></a>
    @*</li>*@
    @*<a href="#"><i class="fa fa-user"></i> Đăng Nhập</a>*@
}
</ul>
